# MCP 服务配置
# DASHSCOPE_API_KEY 从系统环境变量读取，不要写入真实密钥。
enabled: true

servers:
  antv_visualization_chart:
    transport: sse
    url: https://dashscope.aliyuncs.com/api/v1/mcps/antv-visualization-chart/sse
    headers:
      Authorization: Bearer ${DASHSCOPE_API_KEY}
